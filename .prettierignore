# Build output and deps
.next/
out/
build/
node_modules/
coverage/
playwright-report/
test-results/
*.tsbuildinfo
next-env.d.ts

# The prototype is a reference artifact — it must stay byte-identical so that
# `git diff reference/vX.Y/... reference/vZ/...` between drops stays meaningful
# (docs/UPDATING.md step 2). Never reformat it.
reference/

# Generated from the database; regenerated by `supabase gen types`.
lib/types/database.ts

# Plan and execution records — reflowing these churns the diff for no gain.
docs/superpowers/

# Playwright visual baselines
**/*-snapshots/
