# Copy to .env.local and fill in from `npx supabase status` (local CLI stack).
# NOTE: this worktree's stack is remapped off the default ports — API is 54351,
# not 54321, because another worktree's Supabase stack already holds 54321.
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54351
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
